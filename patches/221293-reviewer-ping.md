@banach-space @hanhanW this adds to the precondition of #122927 the two checks that `PadOpVectorizationWithInsertSlicePattern` already has, so a strided or a middle-dim rank-reducing `tensor.insert_slice` is left alone instead of being written to the wrong place. The leading-dim rank-reducing case that the FIXME assumes keeps vectorizing. Would you mind taking a look when you have a moment? Thanks.
